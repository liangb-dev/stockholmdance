# Stockholm Bachata & Salsa

Public dance calendar for Stockholm: today, this week, and upcoming festivals.

## Development

```bash
npm install
npm run dev
```

Open [http://localhost:5180](http://localhost:5180). The calendar feed is proxied from Google ICS at `/api/calendar.ics`.
